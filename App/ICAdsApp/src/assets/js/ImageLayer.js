import Layer from './Layer'
export default class ImageLayer extends Layer {
  constructor(layer) {
    super(layerType)
    this.imageUrl = layer.imageUrl,
    this.imageWidth = layer.imageWidth
    this.imageHeight = layer.imageHeight;
    this.imageHeight = layer.imageHeight;
  }
}